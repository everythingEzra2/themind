import styled from '@emotion/styled';
import tw from 'twin.macro';

export const Button = styled.button`
  ${tw`bg-blue-500 text-gray-100 p-3 rounded-full outline-none focus:outline-none`}
`;

export default Button;
